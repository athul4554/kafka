[project]
name = "stream-store-kafka"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Athul", email = "athulbabu4554@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "confluent-kafka>=2.15.0",
]

[project.scripts]
stream-store-kafka = "stream_store_kafka.producer:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
